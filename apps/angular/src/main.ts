import { bootstrapApplication } from '@angular/platform-browser';
import { AuctionComponent } from './app/auction.component';

bootstrapApplication(AuctionComponent).catch((error) => console.error(error));
